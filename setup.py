from setuptools import setup

setup(name='arc_transformer',
      version='0.1',
      description='Transformer models for the abstraction and reasoning challenge',
      author='Vincent Herrmann',
      license='MIT',
      packages=['arc_transformer'],
      zip_safe=False)